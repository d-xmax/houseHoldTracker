{
  "name": "houseHoldTracker",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "app/*"
  ],
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "format": "pnpm -r run format"

  },
  "devDependencies": {
    "@biomejs/biome": "2.4.7"
  }
}
